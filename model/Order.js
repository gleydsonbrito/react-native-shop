class Order {
    constructor(id, totalPrice, date, products){
        this.id = id,
        this.totalPrice = totalPrice,
        this.date = date,
        this.products = products
    }
}

export default Order;